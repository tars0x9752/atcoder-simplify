{
  "name": "atcoder-simplify",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "build": "webpack --mode development",
    "watch": "webpack --mode development --watch",
    "fmt": "prettier --write src/**/*.ts",
    "prepublish": "webpack --mode production && npm run zip",
    "zip": "rm -rf ./dist.zip && zip -r dist.zip ./dist"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "typescript": "^3.8.3"
  },
  "devDependencies": {
    "clean-webpack-plugin": "^3.0.0",
    "copy-webpack-plugin": "^11.0.0",
    "prettier": "^2.0.5",
    "ts-loader": "^9.4.2",
    "webpack": "^5.75.0",
    "webpack-cli": "^5.0.1"
  }
}
